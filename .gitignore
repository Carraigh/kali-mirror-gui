cat > .gitignore <<'EOF'
# Логи
/var/log/kali-mirror-gui.log
*.log

# Кэш и временные файлы
__pycache__/
*.pyc
*.pyo
*.pyd

# Локальные зависимости (если ты не коммитишь их — но мы коммитим ./lib!)
# Однако, если в будущем захочешь не коммитить ./lib — раскомментируй:
# lib/

# Пользовательские зеркала (локальные)
~/.config/kali-mirror-gui/

# Резервные файлы редакторов
*~
.*.swp
.DS_Store

# Системные резервные копии
/etc/apt/sources.list.bak
EOF
