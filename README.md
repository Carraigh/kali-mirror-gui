# 🔄 Kali Mirror GUI Updater

Графический интерфейс для **автоматического выбора лучшего зеркала Kali Linux**, полного обновления системы и её очистки — **без загрязнения системы глобальными Python-пакетами**.

> 💡 Никаких виртуальных окружений при запуске, никакого PyInstaller — только чистый, локальный и безопасный скрипт с красивым интерфейсом.

---

## ✨ Возможности

- ✅ Автоматически тестирует официальные зеркала Kali Linux через HTTP (не `ping`!)
- ✅ Выбирает **самое быстрое и доступное** зеркало
- ✅ Создаёт резервную копию `/etc/apt/sources.list`
- ✅ Обновляет пакеты: `apt update && apt upgrade`
- ✅ Исправляет зависимости: `apt install -f`
- ✅ Очищает систему: `autoremove`, `autoclean`, `clean`
- ✅ Поддержка **пользовательских зеркал**
- ✅ Современный тёмный интерфейс (`sv-ttk`)
- ✅ Автоматическое добавление в **меню приложений Kali Linux**
- ✅ Режим **CLI fallback**, если GUI недоступен (например, в WSL)
- ✅ Кнопка **«Отмена»** в любой момент

---

## 🚀 Установка

1. **Клонируйте репозиторий** (если ещё не сделали):

git clone https://github.com/Carraigh/kali-mirror-gui.git
cd kali-mirror-gui

2. Установите GUI-зависимость один раз (обязательно):

sudo apt install -y python3-tk

3. Установите локальные Python-зависимости (без вмешательства в систему):

chmod +x install_deps.sh
./install_deps.sh

Скрипт установит requests и sv-ttk в папку ./lib — никаких глобальных пакетов. 

▶️ Запуск

Вариант 1: Через терминал

sudo python3 kali_mirror_gui.py

Вариант 2: Через меню приложений

После установки просто найдите «Kali Mirror Updater» в:

Applications → System Tools 

🛠️ Добавление в меню приложений (вручную)

Если ярлык не появился автоматически:

mkdir -p ~/.local/share/applications/
cp kali-mirror-gui.desktop ~/.local/share/applications/
chmod +x ~/.local/share/applications/kali-mirror-gui.desktop
update-desktop-database ~/.local/share/applications/

Не забудьте отредактировать путь в kali-mirror-gui.desktop, если проект лежит не в /home/neadmin/kali-mirror-gui. 

📁 Структура проекта

kali-mirror-gui/
├── kali_mirror_gui.py      # Основной скрипт (GUI + CLI fallback)
├── install_deps.sh         # Установка локальных зависимостей
├── kali-mirror-gui.desktop # Ярлык для меню приложений
├── lib/                    # Локальные Python-пакеты (requests, sv-ttk)
└── README.md

🔒 Безопасность

Все изменения в /etc/apt/sources.list автоматически бэкапятся → в случае сбоя вы всегда сможете восстановить оригинал.
Скрипт требует root-прав, но не устанавливает ничего в систему (кроме python3-tk, без которого GUI невозможен).

📜 Лицензия
MIT — делайте что угодно, но указывайте авторство 😊

Сделано с ❤️ для пользователей Kali Linux.
Автор: @Carraigh 
